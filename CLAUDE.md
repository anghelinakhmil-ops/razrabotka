# CLAUDE.md — Веб-студия: Разработка сайтов под ключ

> **Референс:** [THE BRIDGE](https://www.thebridge.kyiv.ua)
> **Стиль:** Premium-minimal / Architectural
> **Стек:** Next.js 14+ (App Router), TypeScript, Tailwind CSS, Framer Motion, MDX

---

# 📜 PVZ — ПРАВИЛА ВЫПОЛНЕНИЯ ЗАДАЧ

> **Версия:** 2.0
> **Аббревиатура:** PVZ
> **Статус:** ОБЯЗАТЕЛЬНО К ИСПОЛНЕНИЮ

---

## 🔒 ГЛАВНОЕ ПРАВИЛО

```
⛔ НИКАКАЯ ЗАДАЧА НЕ НАЧИНАЕТСЯ БЕЗ:
   1. Детального плана
   2. Подтверждения от пользователя ("да", "подтверждаю", "начинай")
```

---

## 📊 ТИПЫ ЗАДАЧ И ПОТОКИ

### Тип A: Стандартная задача
```
[ЗАДАЧА] → [АНАЛИЗ] → [ПЛАН] → [ПОДТВЕРЖДЕНИЕ ✋] → [ВЫПОЛНЕНИЕ] → [ПРОВЕРКА] → [ОТЧЁТ]
```

### Тип B: Багфикс (быстрый путь)
```
[БАГ] → [ВОСПРОИЗВЕСТИ] → [ПРИЧИНА] → [ПЛАН ФИКСА] → [ПОДТВЕРЖДЕНИЕ ✋] → [ФИКС] → [ТЕСТ] → [ОТЧЁТ]
```

### Тип C: Hotfix (критический)
```
[КРИТИЧНО] → [БЫСТРЫЙ АНАЛИЗ] → [МИНИМАЛЬНЫЙ ПЛАН] → [ПОДТВЕРЖДЕНИЕ ✋] → [ФИКС] → [ТЕСТ] → [ОТЧЁТ]
```

---

## 📋 ЭТАП 1: АНАЛИЗ ЗАДАЧИ

### 1.1 Понимание контекста
- [ ] Прочитать и понять требования
- [ ] Определить тип задачи (A/B/C)
- [ ] Определить связь с другими задачами/фазами
- [ ] Выявить зависимости (что должно быть готово ДО)
- [ ] Определить что будет зависеть ОТ этой задачи

### 1.2 Исследование
- [ ] Изучить существующий код/файлы
- [ ] Проверить референсы и документацию
- [ ] Изучить best practices
- [ ] Проверить совместимость с текущим стеком

### 1.3 Оценка рисков (для средних/сложных задач)
| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| [Описание] | Низкая/Средняя/Высокая | Низкое/Среднее/Высокое | [Как избежать] |

### 1.4 Проверка на разбиение
**Задачу НЕОБХОДИМО разбить если:**
- [ ] Более 15 шагов выполнения
- [ ] Затрагивает более 10 файлов
- [ ] Содержит несвязанные изменения
- [ ] Смешивает разные типы работ (UI + API + DB)

---

## 📝 ЭТАП 2: ДЕТАЛЬНЫЙ ПЛАН

### Формат плана:

```markdown
## 📋 ПЛАН: [Название задачи]

### Мета-информация
| Параметр | Значение |
|----------|----------|
| Фаза | X.X |
| Тип | Стандартная / Багфикс / Hotfix |
| Сложность | Низкая / Средняя / Высокая |
| Шагов | X |
| Файлов | X создать, X изменить |
| Зависимости | [список или "нет"] |
| Риски | [список или "минимальные"] |

### Шаги выполнения

#### Шаг 1: [Название]
- **Действие:** Что именно будет сделано
- **Файлы:** `path/to/file.tsx`
- **Проверка:** Как убедиться что шаг выполнен

#### Шаг 2: [Название]
...

### Точки контроля (Checkpoints)
- [ ] После шага X — проверить Y
- [ ] После шага X — проверить Y

### Чеклист безопасности (если применимо)
- [ ] Нет XSS уязвимостей
- [ ] Валидация всех inputs
- [ ] Нет секретов в коде

### Критерии успеха
- [ ] Критерий 1
- [ ] Критерий 2

### План отката
Если критическая ошибка:
1. `git checkout -- [files]` или `git reset --hard HEAD~1`
2. [Дополнительные действия]

---
## 🔔 ЗАПРОС НА ПОДТВЕРЖДЕНИЕ

**Готов к выполнению. Жду подтверждения.**

Ответьте:
- `да` / `подтверждаю` — начать выполнение
- `вопрос: ...` — уточнить детали
- `изменить: ...` — внести корректировки
- `отмена` — отменить задачу
---
```

---

## ✅ ЭТАП 3: ПОДТВЕРЖДЕНИЕ

**Варианты ответа пользователя:**

| Ответ | Действие |
|-------|----------|
| `да`, `подтверждаю`, `начинай`, `ок` | Начать выполнение |
| `нет`, `стоп`, `отмена` | Отменить/переработать план |
| `вопрос: ...` | Ответить на вопрос, ждать нового подтверждения |
| `изменить: ...` | Обновить план, ждать нового подтверждения |
| `пропустить шаг X` | Исключить шаг из плана |

---

## ⚡ ЭТАП 4: ВЫПОЛНЕНИЕ

### 4.1 Правила
1. Обновить TodoWrite со всеми шагами
2. Выполнять СТРОГО по плану
3. После каждого шага — краткий статус
4. На checkpoint'ах — проверка
5. При ошибках — СТОП и уведомление

### 4.2 Формат статуса после шага
```markdown
✅ **Шаг X/Y завершён:** [Название]
- Файлы: `path/file.tsx`
- Результат: [краткое описание]
```

### 4.3 При проблеме (не критичной)
```markdown
⚠️ **ПРОБЛЕМА на шаге X:**
- Что случилось: [описание]
- Причина: [почему]
- Варианты:
  1. [Вариант 1]
  2. [Вариант 2]

Жду решения.
```

### 4.4 При критической ошибке
```markdown
🛑 **КРИТИЧЕСКАЯ ОШИБКА — ВЫПОЛНЕНИЕ ОСТАНОВЛЕНО**

- Что случилось: [описание]
- Где: [файл:строка]
- Рекомендация: [что делать]

Активировать план отката? (да/нет)
```

---

## 🔍 ЭТАП 5: ПРОВЕРКА КАЧЕСТВА

### 5.1 Код
- [ ] TypeScript — нет ошибок (`npm run build`)
- [ ] ESLint — нет ошибок (`npm run lint`)
- [ ] Нет `any` типов
- [ ] Нет console.log (кроме dev)
- [ ] Нет закомментированного кода
- [ ] Понятные имена переменных/функций

### 5.2 Функциональность
- [ ] Работает как ожидалось
- [ ] Нет ошибок в консоли браузера
- [ ] Edge cases обработаны

### 5.3 UI/UX (если применимо)
- [ ] Соответствует дизайн-системе
- [ ] Responsive: mobile (375px), tablet (768px), desktop (1280px+)
- [ ] Анимации плавные (60fps)
- [ ] Нет CLS (Cumulative Layout Shift)
- [ ] Hover/focus/active состояния

### 5.4 Безопасность
- [ ] Нет XSS уязвимостей
- [ ] Нет секретов в коде
- [ ] Inputs валидируются
- [ ] Данные санитизируются

### 5.5 Accessibility
- [ ] Семантический HTML
- [ ] Alt для изображений
- [ ] ARIA атрибуты где нужно
- [ ] Keyboard navigation работает
- [ ] Достаточный контраст текста

### 5.6 Производительность
- [ ] Оптимальные импорты (no barrel imports)
- [ ] Изображения оптимизированы (next/image)
- [ ] Lazy loading где нужно

---

## 📄 ЭТАП 6: ДОКУМЕНТИРОВАНИЕ

### 6.1 Обновить TODO.md
- [ ] Отметить задачи `[x]`
- [ ] Обновить счётчики фаз
- [ ] Обновить таблицу прогресса
- [ ] Обновить ИТОГО

### 6.2 Записать решения (если нестандартные)
```markdown
### Принятые решения
- [Дата] [Задача]: [Решение] — [Причина]
```

### 6.3 Записать проблемы (если остались)
```markdown
### Известные проблемы
- [Проблема] — [Планируемое решение]
```

---

## 📊 ЭТАП 7: ОТЧЁТ О ВЫПОЛНЕНИИ

```markdown
---
## ✅ ОТЧЁТ: [Название задачи]

**Статус:** Выполнено / Частично / Заблокировано

### Что сделано:
- [Пункт 1]
- [Пункт 2]

### Файлы:
| Файл | Действие |
|------|----------|
| `path/file.tsx` | Создан / Изменён |

### Проверка качества:
| Категория | Статус |
|-----------|--------|
| Код | ✅ |
| Функциональность | ✅ |
| UI/UX | ✅ / N/A |
| Безопасность | ✅ |
| Accessibility | ✅ / N/A |

### Git:
- Коммит: `[hash]`
- Сообщение: `[message]`

### Прогресс:
- Фаза X.X: [X/X] ✅
- Общий: [X/716 (X%)]

---
Переходим к следующей задаче? (да/нет)
---
```

---

## 🔧 СПЕЦИАЛЬНЫЕ ПРОТОКОЛЫ

### Протокол для UI компонентов
В плане дополнительно указать:
1. Props interface (TypeScript)
2. Все варианты (variants)
3. Все состояния (states)
4. Анимации (если есть)
5. Зависимости от других компонентов

### Протокол для API
В плане дополнительно указать:
1. Endpoint: `METHOD /path`
2. Request schema
3. Response schema
4. Error codes
5. Rate limits (если есть)

### Протокол для работы с секретами
- [ ] Все ключи ТОЛЬКО в `.env`
- [ ] `.env` в `.gitignore`
- [ ] Перед коммитом проверить `git diff` на секреты
- [ ] Использовать `process.env.VARIABLE`

### Протокол для медиа/изображений
- [ ] Формат: WebP/AVIF предпочтительно
- [ ] Использовать `next/image`
- [ ] Указать `width`, `height`, `alt`
- [ ] Добавить `placeholder="blur"` для LCP
- [ ] Оптимизировать размер (< 200KB для hero)

### Протокол Git
- Атомарные коммиты (1 задача = 1 коммит)
- Формат: `type: description`
- Types: `feat`, `fix`, `style`, `refactor`, `docs`, `chore`
- Push после КАЖДОЙ завершённой задачи
- Co-Authored-By в конце сообщения

---

## 📊 УРОВНИ СЛОЖНОСТИ

### Уровень 1: Простая (1-5 шагов)
- Минимальный риск
- Нет зависимостей
- **Требуется:** Краткий план + подтверждение

### Уровень 2: Средняя (6-15 шагов)
- Средний риск
- Есть зависимости
- **Требуется:** Полный план + checkpoints + подтверждение

### Уровень 3: Сложная (15+ шагов)
- Высокий риск
- Много зависимостей
- **Требуется:** Детальный план + оценка рисков + план отката + поэтапное подтверждение

---

## 🎮 КОМАНДЫ ПОЛЬЗОВАТЕЛЯ

| Команда | Действие |
|---------|----------|
| `план` | Показать текущий план |
| `статус` | Текущий статус выполнения |
| `стоп` | Остановить выполнение |
| `откат` | Активировать план отката |
| `пропустить` | Пропустить текущий шаг |
| `детали` | Детали текущего шага |
| `риски` | Показать оценку рисков |
| `прогресс` | Общий прогресс проекта |
| `pvz` | Показать эти правила |

---

## 🚨 МЕХАНИЗМ ЭСКАЛАЦИИ

### Если заблокированы:
1. Описать проблему чётко
2. Показать что уже пробовали
3. Предложить варианты решения
4. Спросить пользователя о решении

### Если нужна информация от пользователя:
```markdown
❓ **НУЖНА ИНФОРМАЦИЯ**

Для продолжения мне нужно знать:
1. [Вопрос 1]?
2. [Вопрос 2]?

Выполнение приостановлено до получения ответа.
```

---

## 📈 МЕТРИКИ УСПЕХА

После каждой сессии оценивать:
- [ ] Все задачи выполнены по плану
- [ ] Нет регрессий (ничего не сломалось)
- [ ] Код проходит все проверки
- [ ] Пользователь подтвердил результат
- [ ] TODO.md актуален

---

## 🔄 ОБРАТНАЯ СВЯЗЬ

После крупных фаз спрашивать:
```markdown
📊 **ОБРАТНАЯ СВЯЗЬ**

Фаза X завершена. Как оцениваете:
1. Качество выполнения? (1-5)
2. Что улучшить в процессе?
3. Есть ли замечания?
```

---

## ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА

1. **НИКОГДА** не начинать без подтверждения
2. **НИКОГДА** не пропускать этапы
3. **ВСЕГДА** обновлять TODO.md
4. **ВСЕГДА** коммитить после задачи
5. **ВСЕГДА** проверять перед коммитом
6. **СТОП** при критической ошибке
7. **СПРОСИТЬ** если что-то непонятно

---

> 📌 **PVZ v2.0** — Этот протокол обязателен. При упоминании "PVZ" — следовать этим правилам.

---
---

## 📋 Содержание документа

1. [PVZ — Правила выполнения задач](#-pvz--правила-выполнения-задач) ⬆️
2. [О проекте](#-о-проекте)
3. [Технический стек](#-технический-стек)
4. [Структура проекта](#-структура-проекта)
5. [Дизайн-система](#-дизайн-система)
6. [Motion-дизайн](#-motion-дизайн)
7. [Протокол деплоя](#-протокол-деплоя-обязательно)
8. [Протокол обновления TODO.md](#-протокол-обновления-todomd-обязательно)
9. [Definition of Done](#-definition-of-done)
10. [Чего НЕ делать](#-чего-не-делать)
11. [SEO требования](#-seo-требования)
12. [Аналитика](#-аналитика-ga4-события)

---

## 🎯 О проекте

### Цель
Коммерческий сайт веб-студии, продающий услугу разработки сайтов под ключ для:
- **Экспертов** — личные бренды, консультации, курсы
- **E-commerce** — интернет-магазины с интеграциями
- **Бизнесов** — лендинги и промо-страницы

### UX-метрики (KPI)
| Метрика | Целевое значение |
|---------|------------------|
| Время до понимания оффера | 10–15 секунд |
| Core Web Vitals (LCP) | < 2.5 секунд |
| Конверсия в заявку | 3–7% |
| Bounce Rate | < 50% |
| Lighthouse Performance | > 90 |

### Карта сайта
```
/                   — Главная (лендинг-галерея)
/about              — Про нас
/services           — Услуги (пакеты)
/cases              — Кейсы (портфолио)
/cases/[slug]       — Детальная страница кейса
/blog               — Блог
/blog/[slug]        — Статья
/contacts           — Контакты
/brief              — Бриф-форма (расширенная заявка)
/privacy            — Политика конфиденциальности
/terms              — Условия использования
```

---

## 🛠️ Технический стек

| Категория | Технология | Обоснование |
|-----------|------------|-------------|
| Framework | Next.js 14+ (App Router) | SSR/SSG, SEO, производительность |
| Language | TypeScript | Типобезопасность, DX |
| Styling | Tailwind CSS | Утилитарный CSS, кастомизация |
| Animation | Framer Motion | Reveal, overlay, loader |
| Content | MDX / Contentlayer | Блог/кейсы без внешнего CMS |
| Forms | React Hook Form + Zod | Валидация |
| Email | Resend / Nodemailer | Отправка заявок |
| Code Quality | ESLint + Prettier | Консистентность |
| Deployment | Vercel | Автодеплой |

### Зависимости (установлены)
```bash
# Основные
next, react, react-dom, typescript

# Анимации
framer-motion

# Контент
@mdx-js/react, @mdx-js/loader, @next/mdx
contentlayer2, next-contentlayer2

# Формы
react-hook-form, zod, @hookform/resolvers

# Email
resend

# Утилиты
clsx, tailwind-merge
```

---

## 📁 Структура проекта

```
├── app/
│   ├── layout.tsx              # Root layout
│   ├── page.tsx                # Главная
│   ├── about/page.tsx
│   ├── services/page.tsx
│   ├── cases/
│   │   ├── page.tsx            # Листинг кейсов
│   │   └── [slug]/page.tsx     # Детальный кейс
│   ├── blog/
│   │   ├── page.tsx            # Листинг статей
│   │   └── [slug]/page.tsx     # Статья
│   ├── contacts/page.tsx
│   ├── brief/page.tsx
│   ├── privacy/page.tsx
│   ├── terms/page.tsx
│   └── api/
│       └── lead/route.ts       # Обработка заявок
│
├── components/
│   ├── layout/                 # Header, Footer, MobileMenu, Preloader
│   ├── ui/                     # Button, Card, Input, Modal...
│   ├── sections/               # Hero, Benefits, FAQ...
│   └── motion/                 # RevealOnScroll, StaggerChildren...
│
├── content/
│   ├── blog/                   # MDX статьи
│   └── cases/                  # MDX кейсы
│
├── lib/                        # Утилиты, хелперы
│
├── public/
│   └── images/                 # Статические изображения
│
├── CLAUDE.md                   # Этот файл (с PVZ)
└── TODO.md                     # Трекинг задач
```

---

## 🎨 Дизайн-система

### Визуальный стиль (DNA)
- **Стиль:** Premium-minimal / Architectural
- **Ощущение:** Галерея, много воздуха, спокойный ритм
- **Акцент:** Типографика + фото, минимум цветов

### Типографика

#### Фирменный приём: «Ломаная» типографика
Смешение кириллицы и латиницы + увеличенная разрядка:
```
Примеры референса: «П R ИРОДА», «O РЕНДИ», «THE B RIDGE»
Наши варианты:     «С A Й Т Ы», «П O Д К Л Ю Ч», «W E B S T U D I O»
```

#### Иерархия
| Элемент | Стиль | Применение |
|---------|-------|------------|
| H1 / Бренд | Display Grotesk, 64–120px, uppercase, tracking широкий | Hero, заголовки секций |
| H2 Секций | 48–64px, uppercase, tracking средний | Названия секций |
| H3 | 24–32px, mixed case | Подзаголовки |
| Body | Neutral Grotesk, 16–18px | Основной текст |
| Нумерация | 48–80px, light | 01–05 как «каркас» |

### Tailwind конфиг (дизайн-токены)
```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        background: '#FFFFFF',
        'background-alt': '#FAFAFA',
        'text-primary': '#1A1A1A',
        'text-muted': '#666666',
        'text-light': '#999999',
        line: '#E5E5E5',
        // accent: '#...' — опционально
      },
      fontFamily: {
        display: ['var(--font-display)', 'sans-serif'],
        body: ['var(--font-body)', 'sans-serif'],
      },
      letterSpacing: {
        'extra-wide': '0.2em',
        'ultra-wide': '0.35em',
      },
      spacing: {
        'section': '120px',
        'section-mobile': '80px',
      }
    }
  }
}
```

### Сетка
- **Desktop:** 12-колоночная сетка
- **Боковые поля:** Крупные (ощущение «галереи»)
- **Контейнер:** `max-w-7xl mx-auto px-6 lg:px-12`
- **Вертикальные отступы:** Большие между секциями

---

## 🎬 Motion-дизайн

### Прелоадер
```typescript
// components/layout/Preloader.tsx
// - Fullscreen overlay
// - Счётчик: 0% → 100%
// - Исчезновение: fade-out + translateY
// - НЕ блокирует SEO
```

### Motion Tokens (Framer Motion)
```typescript
// lib/motion.ts
export const motionConfig = {
  duration: {
    fast: 0.3,
    normal: 0.6,
    slow: 0.9,
  },
  ease: [0.2, 0.8, 0.2, 1], // cubic-bezier
  stagger: {
    fast: 0.08,
    normal: 0.12,
    slow: 0.14,
  },
  translateY: {
    small: 12,
    normal: 20,
    large: 24,
  }
};

export const revealVariants = {
  hidden: {
    opacity: 0,
    y: motionConfig.translateY.normal
  },
  visible: {
    opacity: 1,
    y: 0,
    transition: {
      duration: motionConfig.duration.normal,
      ease: motionConfig.ease,
    }
  }
};
```

### Reveal on Scroll
Порядок появления элементов секции:
1. Номер секции (01–05)
2. Заголовок
3. Изображение
4. Текстовый блок

### Hover-эффекты
- **CTA кнопки:** underline-animate или смена заливки/обводки
- **Навигация:** аккуратный hover, без кислотных эффектов
- **Карточки:** микро-сдвиг 2–4px или subtle scale

### Mobile Menu Overlay
- Открытие/закрытие: slide-in + fade
- Блокировка скролла: `overflow: hidden` на body
- Escape закрывает меню
- Явная кнопка «Закрыть»

---

## 🚀 Протокол деплоя (ОБЯЗАТЕЛЬНО)

После **КАЖДОГО** изменения или добавления функционала:

### 1. Commit & Push
```bash
git add -A
git commit -m "type: описание изменений

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>"
git push origin main
```

### 2. Отчёт для проверки
После пуша **ОБЯЗАТЕЛЬНО** предоставить:

```markdown
**Что изменилось/добавилось:**
- Пункт 1 — описание изменения
- Пункт 2 — описание изменения

**Где проверить:**
- URL страницы для проверки
- Конкретный элемент (Header, Footer, секция, кнопка)

**Ожидаемый результат:**
- Описание того, что должно отображаться
```

### 3. Подтверждение от пользователя
- Пользователь проверяет изменения на production
- **Только после подтверждения** переходим к следующей задаче

### 4. Время деплоя
- Vercel автоматически деплоит после push
- Обычно занимает 1-2 минуты
- Если изменения не видны — подождать и очистить кэш (Ctrl+Shift+R)

### 5. Если деплой упал
1. Открыть логи деплоя в Vercel
2. Найти ошибку
3. Исправить локально
4. Новый commit и push

> ⚠️ **ВАЖНО:** Не переходить к следующей задаче пока деплой не будет успешным!

---

## 📋 Протокол обновления TODO.md (ОБЯЗАТЕЛЬНО)

После **КАЖДОЙ** выполненной задачи:

### 1. Отметить выполненные задачи
```markdown
- [ ] Задача не выполнена
- [x] Задача выполнена  ← изменить на это
```

### 2. Обновить счётчики
- Счётчик секции: `## X.X Название [ВЫПОЛНЕНО/ВСЕГО]`
- Счётчик фазы: `# ФАЗА X: Название [ВЫПОЛНЕНО/ВСЕГО]`
- Таблица прогресса: пересчитать проценты
- ИТОГО: сумма всех выполненных

### 3. Commit
```bash
git add TODO.md
git commit -m "docs: update TODO.md - [краткое описание]"
git push origin main
```

---

## ✅ Definition of Done

### Для каждой страницы
- [ ] Desktop и Mobile responsive
- [ ] SEO meta теги (title, description, OG)
- [ ] Schema.org разметка (если применимо)
- [ ] CTA элементы присутствуют
- [ ] Lighthouse score >= 90
- [ ] Accessibility проверка

### Для каждого компонента
- [ ] TypeScript типизация (без `any`)
- [ ] Все states: default, hover, active, disabled, loading
- [ ] Responsive design
- [ ] Keyboard navigation
- [ ] ARIA labels где нужно

### Для форм
- [ ] Client-side валидация (Zod)
- [ ] Server-side валидация
- [ ] Error states
- [ ] Loading states
- [ ] Success feedback
- [ ] Отправка работает

### Для анимаций
- [ ] Не вызывает CLS
- [ ] Плавная работа на мобильных
- [ ] Respects prefers-reduced-motion

---

## 🚫 Чего НЕ делать

1. ❌ **Не начинать задачу без подтверждения** (PVZ!)
2. ❌ **Не использовать inline styles** — только Tailwind
3. ❌ **Не хардкодить контент** — выносить в константы/MDX
4. ❌ **Не игнорировать TypeScript ошибки** — исправлять сразу
5. ❌ **Не забывать alt для изображений**
6. ❌ **Не создавать страницы без CTA** — коммерческий проект
7. ❌ **Не пушить код с ошибками билда** — проверять локально
8. ❌ **Не использовать `any`** — типизировать всё
9. ❌ **Не забывать про мобильную версию** — mobile-first
10. ❌ **Не делать анимации без ease** — всегда motionConfig.ease
11. ❌ **Не игнорировать a11y** — семантический HTML, aria-labels
12. ❌ **Не коммитить секреты** — только в .env

---

## 📊 SEO требования

### Meta теги (на каждой странице)
```tsx
// app/[page]/page.tsx
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Заголовок страницы | WebStudio',
  description: 'Описание страницы до 160 символов',
  openGraph: {
    title: 'Заголовок для соцсетей',
    description: 'Описание для соцсетей',
    images: ['/og-image.jpg'],
    type: 'website',
  },
  twitter: {
    card: 'summary_large_image',
  },
};
```

### robots.txt
```
User-agent: *
Allow: /
Disallow: /api/

Sitemap: https://your-domain.com/sitemap.xml
```

---

## 📈 Аналитика (GA4 события)

### Настройка
```tsx
// lib/analytics.ts
export const trackEvent = (
  eventName: string,
  params: Record<string, string>
) => {
  if (typeof window !== 'undefined' && window.gtag) {
    window.gtag('event', eventName, params);
  }
};
```

### События
- `cta_click` — клик по CTA
- `form_start` — начало заполнения формы
- `form_submit` — отправка формы
- `form_error` — ошибка формы
- `section_view` — просмотр секции

---

## 🔧 Полезные команды

```bash
npm run dev        # Разработка
npm run build      # Сборка
npm run lint       # Проверка ESLint
npm run start      # Production превью
```

---

## 📝 Шаблон коммит-сообщений

```
feat: добавить секцию Benefits с анимациями
fix: исправить отображение мобильного меню
style: обновить типографику заголовков
refactor: вынести motion-пресеты в отдельный файл
docs: обновить TODO.md
chore: обновить зависимости
```

---

> 📌 **Этот файл — источник истины для проекта. PVZ обязателен к исполнению.**
