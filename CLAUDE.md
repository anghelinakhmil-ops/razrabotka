# CLAUDE.md — Веб-студия: Разработка сайтов под ключ

> **Референс:** [THE BRIDGE](https://www.thebridge.kyiv.ua)
> **Стиль:** Premium-minimal / Architectural
> **Стек:** Next.js 14+ (App Router), TypeScript, Tailwind CSS, Framer Motion, MDX

---

# 📜 PVZ — ПРАВИЛА ВЫПОЛНЕНИЯ ЗАДАЧ

> **Версия:** 3.0
> **Аббревиатура:** PVZ
> **Статус:** ОБЯЗАТЕЛЬНО К ИСПОЛНЕНИЮ
> **Последнее обновление:** 18.01.2026

---

## 🔒 ГЛАВНЫЕ ПРАВИЛА (НАРУШЕНИЕ ЗАПРЕЩЕНО)

```
⛔ ПРАВИЛО 1: НИКАКАЯ ЗАДАЧА НЕ НАЧИНАЕТСЯ БЕЗ:
   1. Детального плана
   2. Подтверждения от пользователя ("да", "подтверждаю", "начинай")

⛔ ПРАВИЛО 2: ПЕРЕХОД К СЛЕДУЮЩЕЙ ЗАДАЧЕ ЗАПРЕЩЁН БЕЗ:
   1. Полного завершения текущей задачи
   2. Успешного деплоя (если были изменения кода)
   3. Предоставления отчёта о выполнении
   4. Подтверждения от пользователя ("следующая", "дальше", "продолжай")

⛔ ПРАВИЛО 3: ЗАПРЕЩЕНО:
   - Начинать задачу без подтверждения
   - Переходить к следующей задаче без подтверждения
   - Пропускать этапы выполнения
   - Коммитить код с ошибками билда
   - Использовать `any` в TypeScript
   - Коммитить секреты (только в .env)
   - Игнорировать TypeScript/ESLint ошибки
   - Забывать alt для изображений
   - Забывать про mobile версию
   - Создавать страницы без CTA
   - Использовать inline styles (только Tailwind)
   - Хардкодить контент (выносить в константы/MDX)
   - Делать анимации без ease (всегда motionConfig.ease)
   - Игнорировать accessibility
```

---

## 📊 ТИПЫ ЗАДАЧ И ПОТОКИ

### Тип A: Стандартная задача
```
[ЗАДАЧА] → [АНАЛИЗ] → [ПЛАН] → [ПОДТВЕРЖДЕНИЕ ✋] → [ВЫПОЛНЕНИЕ] → [ПРОВЕРКА] → [ДЕПЛОЙ] → [ОТЧЁТ] → [ПОДТВЕРЖДЕНИЕ ✋]
```

### Тип B: Багфикс (быстрый путь)
```
[БАГ] → [ВОСПРОИЗВЕСТИ] → [ПРИЧИНА] → [ПЛАН ФИКСА] → [ПОДТВЕРЖДЕНИЕ ✋] → [ФИКС] → [ТЕСТ] → [ДЕПЛОЙ] → [ОТЧЁТ] → [ПОДТВЕРЖДЕНИЕ ✋]
```

### Тип C: Hotfix (критический)
```
[КРИТИЧНО] → [БЫСТРЫЙ АНАЛИЗ] → [МИНИМАЛЬНЫЙ ПЛАН] → [ПОДТВЕРЖДЕНИЕ ✋] → [ФИКС] → [ТЕСТ] → [ДЕПЛОЙ] → [ОТЧЁТ] → [ПОДТВЕРЖДЕНИЕ ✋]
```

---

## 📋 ЭТАП 1: АНАЛИЗ ЗАДАЧИ

### 1.1 Понимание контекста
- [ ] Прочитать и понять требования
- [ ] Определить тип задачи (A/B/C)
- [ ] Определить связь с другими задачами/фазами
- [ ] Выявить зависимости (что должно быть готово ДО)
- [ ] Определить что будет зависеть ОТ этой задачи

### 1.2 Исследование
- [ ] Изучить существующий код/файлы
- [ ] Проверить референсы и документацию
- [ ] Изучить best practices
- [ ] Проверить совместимость с текущим стеком

### 1.3 Оценка рисков (для средних/сложных задач)
| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| [Описание] | Низкая/Средняя/Высокая | Низкое/Среднее/Высокое | [Как избежать] |

### 1.4 Проверка на разбиение
**Задачу НЕОБХОДИМО разбить если:**
- [ ] Более 15 шагов выполнения
- [ ] Затрагивает более 10 файлов
- [ ] Содержит несвязанные изменения
- [ ] Смешивает разные типы работ (UI + API + DB)

---

## 📝 ЭТАП 2: ДЕТАЛЬНЫЙ ПЛАН

### Формат плана:

```markdown
## 📋 ПЛАН: [Название задачи]

### Мета-информация
| Параметр | Значение |
|----------|----------|
| Фаза | X.X |
| Тип | Стандартная / Багфикс / Hotfix |
| Сложность | Низкая / Средняя / Высокая |
| Шагов | X |
| Файлов | X создать, X изменить |
| Зависимости | [список или "нет"] |
| Риски | [список или "минимальные"] |

### Шаги выполнения

#### Шаг 1: [Название]
- **Действие:** Что именно будет сделано
- **Файлы:** `path/to/file.tsx`
- **Проверка:** Как убедиться что шаг выполнен

#### Шаг 2: [Название]
...

### Точки контроля (Checkpoints)
- [ ] После шага X — проверить Y
- [ ] После шага X — проверить Y

### Критерии успеха
- [ ] Критерий 1
- [ ] Критерий 2

### План отката
Если критическая ошибка:
1. `git checkout -- [files]` или `git reset --hard HEAD~1`
2. [Дополнительные действия]

---
## 🔔 ЗАПРОС НА ПОДТВЕРЖДЕНИЕ

**Готов к выполнению. Жду подтверждения.**

Ответьте:
- `да` / `подтверждаю` — начать выполнение
- `вопрос: ...` — уточнить детали
- `изменить: ...` — внести корректировки
- `отмена` — отменить задачу
---
```

---

## ✅ ЭТАП 3: ПОДТВЕРЖДЕНИЕ

**Варианты ответа пользователя:**

| Ответ | Действие |
|-------|----------|
| `да`, `подтверждаю`, `начинай`, `ок` | Начать выполнение |
| `нет`, `стоп`, `отмена` | Отменить/переработать план |
| `вопрос: ...` | Ответить на вопрос, ждать нового подтверждения |
| `изменить: ...` | Обновить план, ждать нового подтверждения |
| `пропустить шаг X` | Исключить шаг из плана |

---

## ⚡ ЭТАП 4: ВЫПОЛНЕНИЕ

### 4.1 Правила выполнения
1. Обновить TodoWrite со всеми шагами
2. Выполнять СТРОГО по плану
3. После каждого шага — краткий статус
4. На checkpoint'ах — проверка
5. При ошибках — СТОП и уведомление

### 4.2 Формат статуса после шага
```markdown
✅ **Шаг X/Y завершён:** [Название]
- Файлы: `path/file.tsx`
- Результат: [краткое описание]
```

### 4.3 При проблеме (не критичной)
```markdown
⚠️ **ПРОБЛЕМА на шаге X:**
- Что случилось: [описание]
- Причина: [почему]
- Варианты:
  1. [Вариант 1]
  2. [Вариант 2]

Жду решения.
```

### 4.4 При критической ошибке
```markdown
🛑 **КРИТИЧЕСКАЯ ОШИБКА — ВЫПОЛНЕНИЕ ОСТАНОВЛЕНО**

- Что случилось: [описание]
- Где: [файл:строка]
- Рекомендация: [что делать]

Активировать план отката? (да/нет)
```

---

## 🔍 ЭТАП 5: ПРОВЕРКА КАЧЕСТВА (ЧЕКЛИСТЫ)

### 5.1 Код
- [ ] `npm run build` — нет ошибок TypeScript
- [ ] `npm run lint` — нет ошибок ESLint
- [ ] Нет `any` типов (ЗАПРЕЩЕНО)
- [ ] Нет `console.log` (кроме dev)
- [ ] Нет закомментированного кода
- [ ] Понятные имена переменных/функций
- [ ] Нет inline styles (только Tailwind)
- [ ] Контент не захардкожен (константы/MDX)

### 5.2 Функциональность
- [ ] Работает как ожидалось
- [ ] Нет ошибок в консоли браузера
- [ ] Edge cases обработаны

### 5.3 UI/UX
- [ ] Соответствует дизайн-системе
- [ ] **Responsive обязательно:**
  - [ ] Mobile: 320px, 375px, 414px
  - [ ] Tablet: 768px, 1024px
  - [ ] Desktop: 1280px, 1440px, 1920px+
- [ ] Анимации плавные (60fps)
- [ ] Нет CLS (Cumulative Layout Shift)
- [ ] Все состояния реализованы:
  - [ ] default
  - [ ] hover
  - [ ] focus (focus-visible ring)
  - [ ] active
  - [ ] disabled
  - [ ] loading (если применимо)

### 5.4 Безопасность
- [ ] Нет XSS уязвимостей
- [ ] Нет секретов в коде (только .env)
- [ ] `.env` в `.gitignore`
- [ ] Inputs валидируются (client + server)
- [ ] Данные санитизируются
- [ ] Перед коммитом: `git diff` — проверить на секреты

### 5.5 Accessibility (a11y)
- [ ] Семантический HTML (nav, main, footer, section, article)
- [ ] Правильная иерархия заголовков (h1 → h2 → h3)
- [ ] Alt для ВСЕХ изображений (информативных)
- [ ] Пустой alt="" для декоративных изображений
- [ ] ARIA атрибуты где нужно:
  - [ ] aria-label для иконок без текста
  - [ ] aria-expanded для accordion
  - [ ] aria-hidden для декоративных элементов
  - [ ] role="dialog" для модалов
  - [ ] aria-live для динамического контента
  - [ ] aria-describedby для полей с ошибками
- [ ] Keyboard navigation работает:
  - [ ] Все интерактивные элементы focusable
  - [ ] Tab order логичный
  - [ ] Focus trap в модалах
  - [ ] Escape закрывает модалы/меню
  - [ ] Enter/Space активирует кнопки
- [ ] Контраст текста WCAG AA (4.5:1 для текста, 3:1 для крупного)
- [ ] Не полагаться только на цвет для информации

### 5.6 Производительность
- [ ] Изображения через `next/image`
- [ ] `placeholder="blur"` для LCP изображений
- [ ] `priority` для hero изображений
- [ ] Размер изображений < 200KB (hero)
- [ ] Форматы: WebP/AVIF предпочтительно
- [ ] Оптимальные импорты (no barrel imports)
- [ ] Lazy loading где нужно

### 5.7 Для страниц (дополнительно)
- [ ] SEO meta теги (title, description, OG)
- [ ] Schema.org разметка (если применимо)
- [ ] CTA элементы присутствуют (коммерческий проект!)
- [ ] Lighthouse score >= 90

### 5.8 Для форм (дополнительно)
- [ ] Client-side валидация (Zod)
- [ ] Server-side валидация
- [ ] Error states отображаются
- [ ] Loading states отображаются
- [ ] Success feedback
- [ ] Отправка работает (email/Telegram)

### 5.9 Для анимаций (дополнительно)
- [ ] Использует motionConfig.ease (ОБЯЗАТЕЛЬНО)
- [ ] Не вызывает CLS
- [ ] Плавная работа на мобильных
- [ ] Respects `prefers-reduced-motion`

---

## 📄 ЭТАП 6: ДОКУМЕНТИРОВАНИЕ

### 6.1 Обновить TODO.md (ОБЯЗАТЕЛЬНО после каждой задачи)

**Шаги:**
1. Отметить выполненные задачи: `- [ ]` → `- [x]`
2. Обновить счётчик секции: `## X.X Название [ВЫПОЛНЕНО/ВСЕГО]`
3. Обновить счётчик фазы: `# ФАЗА X: Название [ВЫПОЛНЕНО/ВСЕГО]`
4. Обновить таблицу прогресса (пересчитать проценты)
5. Обновить ИТОГО (сумма всех выполненных)
6. Обновить статус emoji:
   - ✅ 100% — полностью выполнено
   - 🟡 50-99% — в процессе
   - ❌ 0-49% — не начато

### 6.2 Записать решения (если нестандартные)
```markdown
### Принятые решения
- [Дата] [Задача]: [Решение] — [Причина]
```

### 6.3 Записать проблемы (если остались)
```markdown
### Известные проблемы
- [Проблема] — [Планируемое решение]
```

---

## 🚀 ЭТАП 7: ДЕПЛОЙ

### 7.1 Git Commit (ОБЯЗАТЕЛЬНО после каждой задачи)

**Правила:**
- Атомарные коммиты: 1 задача = 1 коммит
- Push после КАЖДОЙ завершённой задачи
- НИКОГДА не коммитить код с ошибками билда

**Формат коммита:**
```bash
git add -A
git commit -m "$(cat <<'EOF'
type: краткое описание

[Детали если нужно]

Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

**Типы коммитов:**
| Type | Описание | Пример |
|------|----------|--------|
| `feat` | Новый функционал | `feat: add Benefits section with animations` |
| `fix` | Исправление бага | `fix: mobile menu not closing on navigation` |
| `style` | Изменения стилей (не CSS) | `style: update heading typography` |
| `refactor` | Рефакторинг без изменения функционала | `refactor: extract motion presets to separate file` |
| `docs` | Документация | `docs: update TODO.md - Phase 2.1 complete` |
| `chore` | Прочее (зависимости, конфиг) | `chore: update dependencies` |

### 7.2 Проверка деплоя

**После push:**
1. Vercel автоматически деплоит (1-2 минуты)
2. Проверить статус в Vercel Dashboard — должен быть "Ready" ✅
3. Открыть production URL
4. Проверить что изменения применились
5. Проверить консоль браузера (F12) на ошибки

**Если изменения не видны:**
- Подождать 1-2 минуты
- Очистить кэш: Ctrl+Shift+R (или Cmd+Shift+R на Mac)

**Если деплой упал:**
1. Открыть логи деплоя в Vercel
2. Найти ошибку
3. Исправить локально
4. `npm run build` — убедиться что работает
5. Новый commit и push

**⚠️ ВАЖНО:** Не переходить к следующей задаче пока деплой не успешен!

---

## 📊 ЭТАП 8: ОТЧЁТ О ВЫПОЛНЕНИИ

```markdown
---
## ✅ ОТЧЁТ: [Название задачи]

**Статус:** Выполнено / Частично / Заблокировано

### Что сделано:
- [Пункт 1]
- [Пункт 2]

### Файлы:
| Файл | Действие |
|------|----------|
| `path/file.tsx` | Создан / Изменён |

### Проверка качества:
| Категория | Статус |
|-----------|--------|
| Код | ✅ |
| Функциональность | ✅ |
| UI/UX | ✅ / N/A |
| Безопасность | ✅ |
| Accessibility | ✅ / N/A |
| Производительность | ✅ / N/A |

### Деплой:
- Статус: Успешно ✅
- URL: [production URL]

### Git:
- Коммит: `[hash]`
- Сообщение: `[message]`

### Прогресс:
- Фаза X.X: [X/X] ✅
- Общий: [X/716 (X%)]

---
## 🔔 ОЖИДАЮ ПОДТВЕРЖДЕНИЯ

⛔ **Переход к следующей задаче ЗАБЛОКИРОВАН до подтверждения.**

Ответьте:
- `следующая` / `дальше` / `продолжай` — перейти к следующей задаче
- `повторить` — переделать текущую задачу
- `вопрос: ...` — уточнить детали
- `стоп` — остановить работу
---
```

---

## 🔧 СПЕЦИАЛЬНЫЕ ПРОТОКОЛЫ

### Протокол для UI компонентов
В плане дополнительно указать:
1. Props interface (TypeScript)
2. Все варианты (variants): primary, secondary, ghost, outline...
3. Все состояния (states): default, hover, focus, active, disabled, loading
4. Размеры (sizes): sm, md, lg
5. Анимации (если есть)
6. Зависимости от других компонентов

### Протокол для API
В плане дополнительно указать:
1. Endpoint: `METHOD /path`
2. Request schema (Zod)
3. Response schema
4. Error codes и сообщения
5. Rate limits (если есть)
6. Валидация на сервере

### Протокол для секретов
- [ ] Все ключи ТОЛЬКО в `.env`
- [ ] `.env` в `.gitignore` (проверить!)
- [ ] Перед коммитом: `git diff` — искать секреты
- [ ] Использовать `process.env.VARIABLE`
- [ ] НИКОГДА не коммитить: API keys, tokens, passwords, credentials

### Протокол для медиа/изображений
- [ ] Формат: WebP/AVIF предпочтительно
- [ ] Использовать `next/image` (ОБЯЗАТЕЛЬНО)
- [ ] Указать `width`, `height` (ОБЯЗАТЕЛЬНО)
- [ ] Указать `alt` (ОБЯЗАТЕЛЬНО для информативных)
- [ ] `alt=""` для декоративных
- [ ] `placeholder="blur"` для LCP изображений
- [ ] `priority` для hero изображений
- [ ] Размер < 200KB для hero

---

## 📊 УРОВНИ СЛОЖНОСТИ

### Уровень 1: Простая (1-5 шагов)
- Минимальный риск
- Нет зависимостей
- **Требуется:** Краткий план + подтверждение

### Уровень 2: Средняя (6-15 шагов)
- Средний риск
- Есть зависимости
- **Требуется:** Полный план + checkpoints + подтверждение

### Уровень 3: Сложная (15+ шагов)
- Высокий риск
- Много зависимостей
- **Требуется:** Детальный план + оценка рисков + план отката + поэтапное подтверждение

---

## 🎮 КОМАНДЫ ПОЛЬЗОВАТЕЛЯ

| Команда | Действие |
|---------|----------|
| `план` | Показать текущий план |
| `статус` | Текущий статус выполнения |
| `стоп` | Остановить выполнение |
| `откат` | Активировать план отката |
| `пропустить` | Пропустить текущий шаг |
| `детали` | Детали текущего шага |
| `риски` | Показать оценку рисков |
| `прогресс` | Общий прогресс проекта |
| `pvz` | Показать эти правила |
| `следующая` / `дальше` | Подтвердить переход к следующей задаче |
| `повторить` | Переделать текущую задачу |

---

## 🚨 МЕХАНИЗМ ЭСКАЛАЦИИ

### Если заблокированы:
1. Описать проблему чётко
2. Показать что уже пробовали
3. Предложить варианты решения
4. Спросить пользователя о решении

### Если нужна информация от пользователя:
```markdown
❓ **НУЖНА ИНФОРМАЦИЯ**

Для продолжения мне нужно знать:
1. [Вопрос 1]?
2. [Вопрос 2]?

Выполнение приостановлено до получения ответа.
```

---

## 📈 МЕТРИКИ УСПЕХА

После каждой сессии оценивать:
- [ ] Все задачи выполнены по плану
- [ ] Нет регрессий (ничего не сломалось)
- [ ] Код проходит все проверки (build, lint)
- [ ] Деплой успешен
- [ ] Пользователь подтвердил результат
- [ ] TODO.md актуален

---

## 🔄 ОБРАТНАЯ СВЯЗЬ

После крупных фаз спрашивать:
```markdown
📊 **ОБРАТНАЯ СВЯЗЬ**

Фаза X завершена. Как оцениваете:
1. Качество выполнения? (1-5)
2. Что улучшить в процессе?
3. Есть ли замечания?
```

---

> 📌 **PVZ v3.0** — Этот протокол обязателен. При упоминании "PVZ" — следовать этим правилам.

---
---

## 📋 Содержание документа

1. [PVZ — Правила выполнения задач](#-pvz--правила-выполнения-задач) ⬆️
2. [О проекте](#-о-проекте)
3. [Технический стек](#-технический-стек)
4. [Структура проекта](#-структура-проекта)
5. [Дизайн-система](#-дизайн-система)
6. [Motion-дизайн](#-motion-дизайн)
7. [SEO требования](#-seo-требования)
8. [Аналитика](#-аналитика-ga4-события)
9. [Полезные команды](#-полезные-команды)

---

## 🎯 О проекте

### Цель
Коммерческий сайт веб-студии, продающий услугу разработки сайтов под ключ для:
- **Экспертов** — личные бренды, консультации, курсы
- **E-commerce** — интернет-магазины с интеграциями
- **Бизнесов** — лендинги и промо-страницы

### UX-метрики (KPI)
| Метрика | Целевое значение |
|---------|------------------|
| Время до понимания оффера | 10–15 секунд |
| Core Web Vitals (LCP) | < 2.5 секунд |
| Конверсия в заявку | 3–7% |
| Bounce Rate | < 50% |
| Lighthouse Performance | > 90 |

### Карта сайта
```
/                   — Главная (лендинг-галерея)
/about              — Про нас
/services           — Услуги (пакеты)
/cases              — Кейсы (портфолио)
/cases/[slug]       — Детальная страница кейса
/blog               — Блог
/blog/[slug]        — Статья
/contacts           — Контакты
/brief              — Бриф-форма (расширенная заявка)
/privacy            — Политика конфиденциальности
/terms              — Условия использования
```

---

## 🛠️ Технический стек

| Категория | Технология | Обоснование |
|-----------|------------|-------------|
| Framework | Next.js 14+ (App Router) | SSR/SSG, SEO, производительность |
| Language | TypeScript | Типобезопасность, DX |
| Styling | Tailwind CSS v4 | Утилитарный CSS, @theme inline |
| Animation | Framer Motion | Reveal, overlay, loader |
| Content | MDX / Contentlayer2 | Блог/кейсы без внешнего CMS |
| Forms | React Hook Form + Zod | Валидация |
| Email | Resend | Отправка заявок |
| Code Quality | ESLint | Консистентность |
| Deployment | Vercel | Автодеплой |

### Установленные зависимости
```bash
# Основные
next, react, react-dom, typescript

# Анимации
framer-motion

# Контент
@mdx-js/react, @mdx-js/loader, @next/mdx
contentlayer2, next-contentlayer2

# Формы
react-hook-form, zod, @hookform/resolvers

# Email
resend

# Утилиты
clsx, tailwind-merge
```

---

## 📁 Структура проекта

```
├── app/
│   ├── layout.tsx              # Root layout
│   ├── page.tsx                # Главная
│   ├── globals.css             # Tailwind + дизайн-токены
│   ├── about/page.tsx
│   ├── services/page.tsx
│   ├── cases/
│   │   ├── page.tsx            # Листинг кейсов
│   │   └── [slug]/page.tsx     # Детальный кейс
│   ├── blog/
│   │   ├── page.tsx            # Листинг статей
│   │   └── [slug]/page.tsx     # Статья
│   ├── contacts/page.tsx
│   ├── brief/page.tsx
│   ├── privacy/page.tsx
│   ├── terms/page.tsx
│   └── api/
│       └── lead/route.ts       # Обработка заявок
│
├── components/
│   ├── layout/                 # Header, Footer, MobileMenu, Preloader
│   ├── ui/                     # Button, Card, Input, Modal...
│   ├── sections/               # Hero, Benefits, FAQ...
│   └── motion/                 # RevealOnScroll, StaggerChildren...
│
├── content/
│   ├── blog/                   # MDX статьи
│   └── cases/                  # MDX кейсы
│
├── lib/                        # Утилиты, хелперы
│
├── public/
│   └── images/                 # Статические изображения
│
├── CLAUDE.md                   # Этот файл (с PVZ)
└── TODO.md                     # Трекинг задач
```

---

## 🎨 Дизайн-система

### Визуальный стиль (DNA)
- **Стиль:** Premium-minimal / Architectural
- **Ощущение:** Галерея, много воздуха, спокойный ритм
- **Акцент:** Типографика + фото, минимум цветов

### Цветовые токены (globals.css)
```css
:root {
  --color-background: #FFFFFF;
  --color-background-alt: #FAFAFA;
  --color-text-primary: #1a2a32;
  --color-text-secondary: #333333;
  --color-text-muted: #666666;
  --color-text-light: #737373;
  --color-line: #E5E5E5;
  --color-line-dark: #CCCCCC;
  --color-accent: #1a2a32;
  --color-accent-hover: #2a3a42;
}
```

### Типографика

#### Фирменный приём: «Ломаная» типографика
Смешение кириллицы и латиницы + увеличенная разрядка:
```
Примеры референса: «П R ИРОДА», «O РЕНДИ», «THE B RIDGE»
Наши варианты:     «С A Й Т Ы», «П O Д К Л Ю Ч», «W E B S T U D I O»
```

#### Иерархия
| Элемент | Стиль | Применение |
|---------|-------|------------|
| H1 / Бренд | Display, 64–120px, uppercase, tracking широкий | Hero, заголовки секций |
| H2 Секций | 48–64px, uppercase, tracking средний | Названия секций |
| H3 | 24–32px, mixed case | Подзаголовки |
| Body | 16–18px | Основной текст |
| Нумерация | 48–80px, light | 01–05 как «каркас» |

### Сетка
- **Desktop:** 12-колоночная сетка
- **Боковые поля:** Крупные (ощущение «галереи»)
- **Контейнер:** `max-w-7xl mx-auto px-6 lg:px-12`
- **Вертикальные отступы:** Большие между секциями (120px desktop, 80px mobile)

---

## 🎬 Motion-дизайн

### Прелоадер
```typescript
// components/layout/Preloader.tsx
// - Fullscreen overlay
// - Счётчик: 0% → 100%
// - Исчезновение: fade-out + translateY
// - НЕ блокирует SEO
```

### Motion Tokens (Framer Motion)
```typescript
// lib/motion.ts
export const motionConfig = {
  duration: {
    fast: 0.3,
    normal: 0.6,
    slow: 0.9,
  },
  ease: [0.2, 0.8, 0.2, 1], // cubic-bezier — ИСПОЛЬЗОВАТЬ ВСЕГДА
  stagger: {
    fast: 0.08,
    normal: 0.12,
    slow: 0.14,
  },
  translateY: {
    small: 12,
    normal: 20,
    large: 24,
  }
};

export const revealVariants = {
  hidden: { opacity: 0, y: motionConfig.translateY.normal },
  visible: {
    opacity: 1,
    y: 0,
    transition: {
      duration: motionConfig.duration.normal,
      ease: motionConfig.ease,
    }
  }
};
```

### Reveal on Scroll — порядок появления
1. Номер секции (01–05)
2. Заголовок
3. Изображение
4. Текстовый блок

### Hover-эффекты
- **CTA кнопки:** underline-animate или смена заливки/обводки
- **Навигация:** аккуратный hover, без кислотных эффектов
- **Карточки:** микро-сдвиг 2–4px или subtle scale

### Mobile Menu Overlay
- Открытие/закрытие: slide-in + fade
- Блокировка скролла: `overflow: hidden` на body
- Escape закрывает меню
- Явная кнопка «Закрыть»

---

## 📊 SEO требования

### Meta теги (на каждой странице)
```tsx
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Заголовок страницы | WebStudio',
  description: 'Описание страницы до 160 символов',
  openGraph: {
    title: 'Заголовок для соцсетей',
    description: 'Описание для соцсетей',
    images: ['/og-image.jpg'],
    type: 'website',
  },
  twitter: {
    card: 'summary_large_image',
  },
};
```

### robots.txt
```
User-agent: *
Allow: /
Disallow: /api/

Sitemap: https://your-domain.com/sitemap.xml
```

---

## 📈 Аналитика (GA4 события)

```tsx
// lib/analytics.ts
export const trackEvent = (
  eventName: string,
  params: Record<string, string>
) => {
  if (typeof window !== 'undefined' && window.gtag) {
    window.gtag('event', eventName, params);
  }
};
```

### События
- `cta_click` — клик по CTA (location, text, page)
- `form_start` — начало заполнения формы
- `form_submit` — отправка формы
- `form_error` — ошибка формы
- `section_view` — просмотр секции

---

## 🔧 Полезные команды

```bash
npm run dev        # Разработка
npm run build      # Сборка (проверка TypeScript)
npm run lint       # Проверка ESLint
npm run start      # Production превью
```

---

> 📌 **Этот файл — источник истины для проекта. PVZ v3.0 обязателен к исполнению.**
