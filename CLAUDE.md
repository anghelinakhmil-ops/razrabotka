# CLAUDE.md — Веб-студия: Разработка сайтов под ключ

> **Референс:** [THE BRIDGE](https://www.thebridge.kyiv.ua)  
> **Стиль:** Premium-minimal / Architectural  
> **Стек:** Next.js 14+ (App Router), TypeScript, Tailwind CSS, Framer Motion, MDX

---

## 📋 Содержание

1. [О проекте](#-о-проекте)
2. [Технический стек](#-технический-стек)
3. [Структура проекта](#-структура-проекта)
4. [Дизайн-система](#-дизайн-система)
5. [Motion-дизайн](#-motion-дизайн)
6. [Протокол деплоя](#-протокол-деплоя-обязательно)
7. [Протокол обновления TODO.md](#-протокол-обновления-todomd-обязательно)
8. [Definition of Done](#-definition-of-done)
9. [Чего НЕ делать](#-чего-не-делать)
10. [SEO требования](#-seo-требования)
11. [Аналитика](#-аналитика-ga4-события)

---

## 🎯 О проекте

### Цель
Коммерческий сайт веб-студии, продающий услугу разработки сайтов под ключ для:
- **Экспертов** — личные бренды, консультации, курсы
- **E-commerce** — интернет-магазины с интеграциями
- **Бизнесов** — лендинги и промо-страницы

### UX-метрики (KPI)
| Метрика | Целевое значение |
|---------|------------------|
| Время до понимания оффера | 10–15 секунд |
| Core Web Vitals (LCP) | < 2.5 секунд |
| Конверсия в заявку | 3–7% |
| Bounce Rate | < 50% |
| Lighthouse Performance | > 90 |

### Карта сайта
```
/                   — Главная (лендинг-галерея)
/about              — Про нас
/services           — Услуги (пакеты)
/cases              — Кейсы (портфолио)
/cases/[slug]       — Детальная страница кейса
/blog               — Блог
/blog/[slug]        — Статья
/contacts           — Контакты
/brief              — Бриф-форма (расширенная заявка)
/privacy            — Политика конфиденциальности
/terms              — Условия использования
```

---

## 🛠️ Технический стек

| Категория | Технология | Обоснование |
|-----------|------------|-------------|
| Framework | Next.js 14+ (App Router) | SSR/SSG, SEO, производительность |
| Language | TypeScript | Типобезопасность, DX |
| Styling | Tailwind CSS | Утилитарный CSS, кастомизация |
| Animation | Framer Motion | Reveal, overlay, loader |
| Content | MDX / Contentlayer | Блог/кейсы без внешнего CMS |
| Forms | React Hook Form + Zod | Валидация |
| Email | Resend / Nodemailer | Отправка заявок |
| Code Quality | ESLint + Prettier | Консистентность |
| Deployment | Vercel | Автодеплой |

### Зависимости (обязательные)
```bash
npm install framer-motion
npm install @mdx-js/react @mdx-js/loader
npm install react-hook-form zod @hookform/resolvers
npm install resend  # или nodemailer
```

---

## 📁 Структура проекта

```
├── app/
│   ├── layout.tsx              # Root layout
│   ├── page.tsx                # Главная
│   ├── about/page.tsx
│   ├── services/page.tsx
│   ├── cases/
│   │   ├── page.tsx            # Листинг кейсов
│   │   └── [slug]/page.tsx     # Детальный кейс
│   ├── blog/
│   │   ├── page.tsx            # Листинг статей
│   │   └── [slug]/page.tsx     # Статья
│   ├── contacts/page.tsx
│   ├── brief/page.tsx
│   ├── privacy/page.tsx
│   ├── terms/page.tsx
│   └── api/
│       └── lead/route.ts       # Обработка заявок
│
├── components/
│   ├── layout/
│   │   ├── Header.tsx
│   │   ├── Footer.tsx
│   │   ├── MobileMenu.tsx      # Fullscreen overlay
│   │   └── Preloader.tsx       # 0–100%
│   ├── ui/
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Input.tsx
│   │   └── ...
│   ├── sections/               # Секции главной
│   │   ├── Hero.tsx
│   │   ├── Benefits.tsx        # 01–05
│   │   ├── Metrics.tsx
│   │   ├── Services.tsx
│   │   ├── Process.tsx
│   │   ├── CasesPreview.tsx
│   │   ├── Testimonials.tsx
│   │   ├── FAQ.tsx
│   │   └── LeadForm.tsx
│   └── motion/
│       ├── RevealOnScroll.tsx
│       ├── StaggerChildren.tsx
│       └── motionPresets.ts    # Токены анимаций
│
├── content/
│   ├── blog/                   # MDX статьи
│   └── cases/                  # MDX кейсы
│
├── lib/
│   ├── motion.ts               # Framer Motion утилиты
│   ├── validation.ts           # Zod схемы
│   └── api.ts                  # API хелперы
│
├── styles/
│   └── globals.css             # Tailwind + кастомные стили
│
├── public/
│   ├── images/
│   └── fonts/
│
├── TODO.md                     # Трекинг задач
└── CLAUDE.md                   # Этот файл
```

---

## 🎨 Дизайн-система

### Визуальный стиль (DNA)
- **Стиль:** Premium-minimal / Architectural
- **Ощущение:** Галерея, много воздуха, спокойный ритм
- **Акцент:** Типографика + фото, минимум цветов

### Типографика

#### Фирменный приём: «Ломаная» типографика
Смешение кириллицы и латиницы + увеличенная разрядка:
```
Примеры референса: «П R ИРОДА», «O РЕНДИ», «THE B RIDGE»
Наши варианты:     «С A Й Т Ы», «П O Д К Л Ю Ч», «W E B S T U D I O»
```

#### Иерархия
| Элемент | Стиль | Применение |
|---------|-------|------------|
| H1 / Бренд | Display Grotesk, 64–120px, uppercase, tracking широкий | Hero, заголовки секций |
| H2 Секций | 48–64px, uppercase, tracking средний | Названия секций |
| H3 | 24–32px, mixed case | Подзаголовки |
| Body | Neutral Grotesk, 16–18px | Основной текст |
| Нумерация | 48–80px, light | 01–05 как «каркас» |

### Tailwind конфиг (дизайн-токены)
```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        background: '#FFFFFF',
        'background-alt': '#FAFAFA',
        'text-primary': '#1A1A1A',
        'text-muted': '#666666',
        'text-light': '#999999',
        line: '#E5E5E5',
        // accent: '#...' — опционально
      },
      fontFamily: {
        display: ['var(--font-display)', 'sans-serif'],
        body: ['var(--font-body)', 'sans-serif'],
      },
      letterSpacing: {
        'extra-wide': '0.2em',
        'ultra-wide': '0.35em',
      },
      spacing: {
        'section': '120px',
        'section-mobile': '80px',
      }
    }
  }
}
```

### Сетка
- **Desktop:** 12-колоночная сетка
- **Боковые поля:** Крупные (ощущение «галереи»)
- **Контейнер:** `max-w-7xl mx-auto px-6 lg:px-12`
- **Вертикальные отступы:** Большие между секциями

---

## 🎬 Motion-дизайн

### Прелоадер
```typescript
// components/layout/Preloader.tsx
// - Fullscreen overlay
// - Счётчик: 0% → 100%
// - Исчезновение: fade-out + translateY
// - НЕ блокирует SEO
```

### Motion Tokens (Framer Motion)
```typescript
// lib/motion.ts
export const motionConfig = {
  duration: {
    fast: 0.3,
    normal: 0.6,
    slow: 0.9,
  },
  ease: [0.2, 0.8, 0.2, 1], // cubic-bezier
  stagger: {
    fast: 0.08,
    normal: 0.12,
    slow: 0.14,
  },
  translateY: {
    small: 12,
    normal: 20,
    large: 24,
  }
};

export const revealVariants = {
  hidden: { 
    opacity: 0, 
    y: motionConfig.translateY.normal 
  },
  visible: { 
    opacity: 1, 
    y: 0,
    transition: {
      duration: motionConfig.duration.normal,
      ease: motionConfig.ease,
    }
  }
};
```

### Reveal on Scroll
Порядок появления элементов секции:
1. Номер секции (01–05)
2. Заголовок
3. Изображение
4. Текстовый блок

### Hover-эффекты
- **CTA кнопки:** underline-animate или смена заливки/обводки
- **Навигация:** аккуратный hover, без кислотных эффектов
- **Карточки:** микро-сдвиг 2–4px или subtle scale

### Mobile Menu Overlay
- Открытие/закрытие: slide-in + fade
- Блокировка скролла: `overflow: hidden` на body
- Escape закрывает меню
- Явная кнопка «Закрыть»

---

## 🚀 Протокол деплоя (ОБЯЗАТЕЛЬНО)

После **КАЖДОГО** изменения или добавления функционала:

### 1. Commit & Push
```bash
git add -A
git commit -m "Описание изменений"
git push origin main
```

### 2. Отчёт для проверки
После пуша **ОБЯЗАТЕЛЬНО** предоставить:

```markdown
**Что изменилось/добавилось:**
- Пункт 1 — описание изменения
- Пункт 2 — описание изменения

**Где проверить:**
- URL страницы для проверки
- Конкретный элемент (Header, Footer, секция, кнопка)

**Ожидаемый результат:**
- Описание того, что должно отображаться
```

### 3. Подтверждение от пользователя
- Пользователь проверяет изменения на production
- Пользователь отправляет скриншот для подтверждения
- **Только после подтверждения** переходим к следующей задаче

### 4. Время деплоя
- Vercel автоматически деплоит после push
- Обычно занимает 1-2 минуты
- Если изменения не видны — подождать и очистить кэш (Ctrl+Shift+R)

### 5. Проверка успешности деплоя

#### а) Dashboard хостинга
- Последний деплой имеет статус "Ready" ✅
- Нет ошибок билда

#### б) Проверка через сайт
1. Открыть production URL
2. Проверить что изменения применились
3. Проверить консоль браузера (F12 → Console) на ошибки

#### в) Если деплой упал
1. Открыть логи деплоя
2. Найти ошибку в логах билда
3. Исправить ошибку локально
4. Сделать новый commit и push
5. Повторить проверку

#### г) Типичные ошибки билда
| Ошибка | Решение |
|--------|---------|
| TypeScript errors | Исправить типы |
| ESLint errors | Исправить линтер ошибки |
| Import errors | Проверить пути импортов |
| Build errors | Проверить конфигурацию |

> ⚠️ **ВАЖНО:** Не переходить к следующей задаче пока деплой не будет успешным!

---

## 📋 Протокол обновления TODO.md (ОБЯЗАТЕЛЬНО)

После **КАЖДОЙ** выполненной задачи необходимо актуализировать TODO.md:

### 1. Когда обновлять
- После завершения любой задачи из TODO.md
- После исправления багов
- После добавления нового функционала
- В конце рабочей сессии

### 2. Что обновлять

#### а) Отметить выполненные задачи
```markdown
- [ ] Задача не выполнена
- [x] Задача выполнена  ← изменить на это
```

#### б) Обновить счётчики фаз
```markdown
# Фаза X: Название [ВЫПОЛНЕНО/ВСЕГО]
```

#### в) Обновить таблицу прогресса
- Пересчитать процент: `(выполнено / всего) * 100`
- Обновить emoji статус:
  - ✅ 100% — полностью выполнено
  - 🟡 50-99% — в процессе  
  - ❌ 0-49% — не начато

#### г) Обновить общий прогресс
Сумма всех выполненных задач из всех фаз.

### 3. Commit после обновления
```bash
git add TODO.md
git commit -m "Update TODO.md - краткое описание"
git push origin main
```

### 4. Важно
- ❌ НЕ отмечать задачу выполненной, если она не завершена полностью
- ❌ НЕ забывать обновлять ВСЕ счётчики
- ✅ ВСЕГДА коммитить изменения TODO.md

---

## ✅ Definition of Done

### Для каждой страницы
- [ ] Desktop и Mobile responsive
- [ ] SEO meta теги (title, description, OG)
- [ ] Schema.org разметка (если применимо)
- [ ] CTA элементы присутствуют
- [ ] Lighthouse score >= 90
- [ ] Accessibility (a11y) проверка

### Для каждого компонента
- [ ] TypeScript типизация
- [ ] Все states реализованы (default, hover, active, disabled, loading)
- [ ] Responsive design
- [ ] Keyboard navigation
- [ ] ARIA labels где нужно

### Для форм
- [ ] Client-side валидация (Zod)
- [ ] Server-side валидация
- [ ] Error states
- [ ] Loading states
- [ ] Success feedback
- [ ] Отправка работает (email/Telegram)

### Для анимаций
- [ ] Не вызывает CLS
- [ ] Плавная работа на мобильных
- [ ] Respects prefers-reduced-motion

---

## 🚫 Чего НЕ делать

1. ❌ **Не использовать inline styles** — только Tailwind
2. ❌ **Не хардкодить контент** — выносить в константы/MDX
3. ❌ **Не игнорировать TypeScript ошибки** — исправлять сразу
4. ❌ **Не забывать alt для изображений**
5. ❌ **Не создавать страницы без CTA** — коммерческий проект
6. ❌ **Не пушить код с ошибками билда** — проверять локально
7. ❌ **Не использовать `any`** — типизировать всё
8. ❌ **Не забывать про мобильную версию** — mobile-first
9. ❌ **Не делать анимации без ease** — всегда использовать motionConfig.ease
10. ❌ **Не игнорировать a11y** — семантический HTML, aria-labels

---

## 📊 SEO требования

### Meta теги (на каждой странице)
```tsx
// app/[page]/page.tsx
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Заголовок страницы | WebStudio',
  description: 'Описание страницы до 160 символов',
  openGraph: {
    title: 'Заголовок для соцсетей',
    description: 'Описание для соцсетей',
    images: ['/og-image.jpg'],
    type: 'website',
  },
  twitter: {
    card: 'summary_large_image',
  },
};
```

### robots.txt
```
User-agent: *
Allow: /
Disallow: /api/
Disallow: /brief  # опционально

Sitemap: https://your-domain.com/sitemap.xml
```

### sitemap.xml
```tsx
// app/sitemap.ts
import { MetadataRoute } from 'next';

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = 'https://your-domain.com';
  
  return [
    { url: baseUrl, lastModified: new Date(), priority: 1 },
    { url: `${baseUrl}/about`, lastModified: new Date(), priority: 0.8 },
    { url: `${baseUrl}/services`, lastModified: new Date(), priority: 0.9 },
    { url: `${baseUrl}/cases`, lastModified: new Date(), priority: 0.8 },
    { url: `${baseUrl}/blog`, lastModified: new Date(), priority: 0.7 },
    { url: `${baseUrl}/contacts`, lastModified: new Date(), priority: 0.6 },
  ];
}
```

---

## 📈 Аналитика (GA4 события)

### Настройка
```tsx
// lib/analytics.ts
export const trackEvent = (
  eventName: string, 
  params: Record<string, string>
) => {
  if (typeof window !== 'undefined' && window.gtag) {
    window.gtag('event', eventName, params);
  }
};
```

### События для отслеживания

#### CTA клики
```typescript
trackEvent('cta_click', {
  cta_location: 'hero' | 'header' | 'footer' | 'section',
  cta_text: 'ОБСУДИТЬ ПРОЕКТ',
  page_path: '/services'
});
```

#### Формы
```typescript
trackEvent('form_start', { form_name: 'quick_lead' | 'brief' });
trackEvent('form_submit', { form_name: 'quick_lead' | 'brief' });
trackEvent('form_error', { form_name: string, error: string });
```

#### Скролл секций
```typescript
trackEvent('section_view', { 
  section_name: 'benefits' | 'cases' | 'faq',
  section_index: '01' 
});
```

---

## 🔧 Полезные команды

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Проверка типов
npm run type-check

# Линтер
npm run lint

# Форматирование
npm run format

# Превью production билда
npm run start
```

---

## 📝 Шаблон коммит-сообщений

```
feat: добавить секцию Benefits с анимациями
fix: исправить отображение мобильного меню
style: обновить типографику заголовков
refactor: вынести motion-пресеты в отдельный файл
docs: обновить TODO.md
chore: обновить зависимости
```

---

## 🎯 Быстрый старт новой задачи

1. **Прочитать задачу** в TODO.md
2. **Проверить зависимости** — нужны ли предыдущие задачи
3. **Создать/изменить файлы**
4. **Проверить локально** — `npm run dev`
5. **Проверить билд** — `npm run build`
6. **Commit & Push** — следовать протоколу деплоя
7. **Дождаться подтверждения** пользователя
8. **Обновить TODO.md**
9. **Перейти к следующей задаче**

---

> 📌 **Этот файл — источник истины для проекта. Обновляй его при изменении требований.**
